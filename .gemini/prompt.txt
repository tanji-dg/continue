英語で考え、日本語で出力する。
思考状況は日本語で出力する。

次のタスクを直ちに実行する。

- git fetch originを実行
- origin/mainブランチに変更がなければ終了
- 現状のブランチで修正があれば一旦コミット
- dg/rebase/YYMMDDのフォーマットでブランチを作成
- git rebase origin/mainを完遂
- scripts/install-dependencies.shを完遂
- cd extensions/vscodeして、次のコマンドを完遂
  - npm run build:prod
  - npm run package-all
- git push githubを実行
- 新たに生成したパッケージを表示
