# Gemini ワークスペースコンテキスト

このドキュメントは、Gemini AIモデル向けのプロジェクトに関するコンテキストを提供します。

## プロジェクト概要

Continueは、開発者がIDE内でカスタムAIアシスタントを作成、共有、使用できるようにするオープンソースのAIコードアシスタントです。VS CodeおよびJetBrainsの拡張機能として利用可能で、モデル、ルール、プロンプト、その他のビルディングブロックのハブによってサポートされています。

### 主な機能

- **エージェント**: コードベースに大幅な変更を加えます。
- **チャット**: IDEを離れることなくLLMから助けを得られます。
- **オートコンプリート**: 入力中にインラインでコードを提案します。
- **編集**: 現在のファイル内のコードを便利に修正します。

### アーキテクチャ

これは、4つの主要なサブプロジェクトで構成されるTypeScriptプロジェクトのモノレポです。

- **`core`**: 異なる環境で共有されるアプリケーションのコアロジックを含みます。
- **`gui`**: ReactとViteで構築されたアプリケーションのユーザーインターフェース。
- **`extensions/vscode`**: VS Code拡張機能。
- **`binary`**: アプリケーションのバイナリコンポーネント。

## 開発セットアップ

### 前提条件

- **Node.js**: バージョン20.19.0 (LTS) 以降。NVMを使用している場合は、ルートディレクトリで `nvm use` を実行して正しいバージョンに切り替えてください。
- **Vite**: Viteをグローバルにインストールします: `npm i -g vite`。

### インストール

VS Codeのコマンドパレット（`Ctrl+Shift+P`）から `install-all-dependencies` タスクを実行するか、ルートディレクトリと各サブディレクトリ（`core`、`gui`、`extensions/vscode`、`binary`）で `npm install` を実行します。

### デバッグ (VS Code)

1.  「実行とデバッグ」ビューを開きます。
2.  ドロップダウンメニューから `Launch extension` を選択します。
3.  再生ボタンを押して、新しい「ホスト」VS Codeウィンドウで拡張機能を開始します。
4.  `gui`、`core`、または `extensions/vscode` への変更は、ホストウィンドウで自動的にホットリロードされます。`core` と `extensions/vscode` の変更については、ウィンドウをリロードする必要がある場合があります（`Ctrl+Shift+P` -> "Reload Window"）。

## 一般的なタスク

### プロジェクトのビルド

- **本番ビルド（最適化済み）**: リリース用に小型で最適化されたビルドを作成するには、ルートディレクトリから次のコマンドを実行します。これにより、デバッグ情報が削除され、ファイルが最小化されます。
  ```bash
  npm run build:prod
  ```
- **開発ビルド**: 開発用に、個別のコンポーネントをビルドできます。たとえば、デバッグ用にソースマップ付きでVS Code拡張機能をビルドするには、次のようにします。
  ```bash
  cd extensions/vscode
  npm run esbuild
  ```

### テストの実行

テストはJestとVitestの両方を使用して記述されています。

- サブプロジェクトのすべてのテストを実行するには、そのディレクトリに `cd` して `npm test` を実行します。
- 一部のサブプロジェクトには、Vitestテストのみを実行するための `vitest` スクリプトもあります。

たとえば、`core` サブプロジェクトのテストを実行するには、次のようにします。
```bash
cd core
npm test
```

### フォーマットとリンティング

- **フォーマット**: このプロジェクトではPrettierを使用しています。コードベース全体をフォーマットするには、次のようにします。
  ```bash
  npm run format
  ```
- **リンティング**: このプロジェクトではESLintを使用しています。特定のサブプロジェクトをリントするには、次のようにします。
  ```bash
  cd core
  npm run lint
  ```

### 拡張機能のパッケージング

VS Code拡張機能をインストール用に `.vsix` ファイルにパッケージングするには、次のようにします。

1.  `extensions/vscode` ディレクトリに移動します。
2.  本番ビルドを実行します: `npm run build:prod`
3.  パッケージングスクリプトを実行します: `npm run package`
    出力は `extensions/vscode/build/continue-{VERSION}.vsix` にあります。

### ドキュメンテーションサーバーの実行

ドキュメンテーションウェブサイトをローカルで実行するには、次のようにします。

1.  `docs` ディレクトリに移動します。
2.  依存関係をインストールします: `npm install`
3.  サーバーを起動します: `npm run start`
    ドキュメントは `http://localhost:3000` で利用可能になります。

## コントリビューションガイドライン

- **Gitワークフロー**: `main` から機能ブランチを作成し、`main` に対してプルリクエストを開きます。
- **プルリクエスト**: コードを書く前に、Issueを開くかコメントしてください。PRは単一の変更に焦点を当ててください。
- **CLA**: すべての貢献者は、プルリクエストにコメントすることで貢献者ライセンス契約に署名する必要があります。
